Side effects: 
  const [count, setCount] = useState(0);

  function increaseCount() {
    setCount(count + 1);
  }

  setInterval(increaseCount, 1000);

  return <div>
    <div>Count: {count} </div>
  </div>


